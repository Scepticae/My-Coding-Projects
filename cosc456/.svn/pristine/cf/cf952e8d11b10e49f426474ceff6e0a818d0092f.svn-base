#include<stdlib.h>
#include"jmesh.h"
#include"offreader.h"

jmesh * 
new_jmesh(FILE * fp)
{
	int check = 0;
  jmesh * jm;

  jm = (jmesh*)malloc(sizeof(jmesh));

  check = load_off_mesh(fp, jm);
	
	if(check == -1){jm = NULL;}
	
  return jm;
}

void free_mesh(jmesh *jm){
	;
}